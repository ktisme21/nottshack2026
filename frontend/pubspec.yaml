name: esg_supply_chain
description: ESG Supply Chain Platform with Blockchain Verification
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  firebase_core: ^2.24.2
  cloud_firestore: ^4.14.0
  
  # API
  http: ^1.1.0
  
  # UI
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.6
  fl_chart: ^0.65.0
  
  # Utilities
  intl: ^0.18.1
  provider: ^6.1.1
  pdf: ^3.12.0
  printing: ^5.14.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
