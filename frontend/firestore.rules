rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read and write access for development
    // Warning: These rules are open to anyone and should be replaced before production
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2026, 5, 4);
    }
  }
}
